- if user_signed_in?
  %p#intro
    Hello,
    = current_user.name
    %br/
    %span
      Share your inspiration and see what's inspiring others.
- else
  %p#intro
    What's your muse?
    %br/
    %span
      Share your inspiration and see what's inspiring others.

- @posts.each do |post|
  = link_to (image_tag post.image.url)
  %h2= link_to post.title, post
  %p
    = pluralize(post.comments.count, "Comment")
  %p
    = pluralize(post.get_likes.count, "Like")


= link_to "Add New Inspiration", new_post_path
